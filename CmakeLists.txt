cmake_minimum_required (VERSION 2.4)
project (tree_structures)
include_directories (. ./tree_structures)
file(GLOB_RECURSE SRC_LIST ./src/tree_structures/*.cpp ./src/tree_structures/*.c)
message(STATUS "=============" ${CMAKE_HOME_DIRECTORY})
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY lib)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY lib)
link_directories(/lib)
add_library(tree_structures ${SRC_LIST})
link_libraries(tree_structures)
add_executable(main src/TreeStructures.cpp)